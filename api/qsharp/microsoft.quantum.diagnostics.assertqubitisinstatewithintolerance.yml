### YamlMime:QSharpType
uid: microsoft.quantum.diagnostics.assertqubitisinstatewithintolerance
name: AssertQubitIsInStateWithinTolerance
type: operation
namespace: Microsoft.Quantum.Diagnostics
summary: >-
  Valida que qubit en el estado esperado.


  `expected`representa un vector complejo, $ \ket{\psi} = \begin{bmatrix}a & b\end {bmatrix} ^ {\mathrm{T}} $.

  El primer elemento de las tuplas que representa cada una de $a $, $b $ es la parte real del número complejo, mientras que la segunda es la parte imaginaria.

  El último argumento define la tolerancia con la que se realiza la aserción.
remarks: >-
  El siguiente código de Mathematica se puede usar para comprobar las expresiones de mi, mx, My, MZ:


  ```mathematica

  {Id, X, Y, Z} = Table[PauliMatrix[k], {k, 0, 3}];

  st = {{ reA + I imA }, { reB + I imB} };

  M = st . ConjugateTranspose[st];

  mx = Tr[M.X] // ComplexExpand;

  my = Tr[M.Y] // ComplexExpand;

  mz = Tr[M.Z] // ComplexExpand;

  mi = Tr[M.Id] // ComplexExpand;

  2 m == Id mi + X mx + Z mz + Y my // ComplexExpand // Simplify

  ```


  La tolerancia es la $L\_{\infty} $ distance entre 3 vectores reales (x ₂, x ₃, x ₄) definido por $ \langle\psi | \psi\rangle = x\_1 I + x\_2 x + x\_3 y + x\_4 Z $ y Vector real (y ₂, y ₃, y ₄) definidos por p = Y ₁ I + y ₂ x + y ₃ y + y ₄ Z, donde p es la matriz de densidad que corresponde al estado del registro.

  Esto solo se aplica en la suposición de que TR (p) y TR (| ψ ⟩ ⟨ ψ |) son 1 (por ejemplo, x ₁ = 1/2, y ₁ = 1/2).

  Si no es así, la función garantiza que la distancia l ∞ entre (x ₂-x ₁, x ₃-x ₁, x ₄-x ₁, x ₄ + x ₁) y (y ₂-y ₁, y ₃-y ₁, y ₄-y ₁, y ₄ + y ₁) es menor que el parámetro Tolerance.
examples: >-
  ```Q#

  using (qubits = Qubit[2]) {
      // Both qubits are initialized as |0〉: a=(1 + 0*i), b=(0 + 0*i)
      AssertQubitIsInStateWithinTolerance((Complex(1., 0.), Complex(0., 0.)), qubits[0], 1e-5);
      AssertQubitIsInStateWithinTolerance((Complex(1., 0.), Complex(0., 0.)), qubits[1], 1e-5);
      Y(qubits[1]);
      // Y |0〉 = i |1〉: a=(0 + 0*i), b=(0 + 1*i)
      AssertQubitIsInStateWithinTolerance((Complex(0., 0.), Complex(0., 1.)), qubits[1], 1e-5);
  }

  ```
syntax: 'operation AssertQubitIsInStateWithinTolerance (expected : (Microsoft.Quantum.Math.Complex, Microsoft.Quantum.Math.Complex), register : Qubit, tolerance : Double) : Unit'
input:
  content: '(expected : (Microsoft.Quantum.Math.Complex, Microsoft.Quantum.Math.Complex), register : Qubit, tolerance : Double)'
  types:
  - name: expected
    summary: Se esperaban las amplitudes complejas{0}de $ \ket ${1}y $ \ket $, respectivamente.
    types:
    - uid: microsoft.quantum.math.complex
    - uid: microsoft.quantum.math.complex
  - name: register
    summary: Qubit cuyo estado se va a declarar. Tenga en cuenta que se supone que este qubit se puede separar de otros qubits asignados y no está insuficiente.
    isPrimitive: true
    uid: Qubit
  - name: tolerance
    summary: >-
      Tolerancia aditiva mediante la cual se permite que las amplitudes reales se desvíen de lo esperado.

      Vea la sección Comentarios a continuación para obtener más información.
    isPrimitive: true
    uid: Double
output:
  content: Unit
  types:
  - isPrimitive: true
    uid: Unit
metadata:
  ms.openlocfilehash: 77d28c122a44f39924242dedb6bd6163974e18c1
  ms.sourcegitcommit: db23885adb7ff76cbf8bd1160d401a4f0471e549
  ms.translationtype: MT
  ms.contentlocale: es-ES
  ms.lasthandoff: 05/01/2020
  ms.locfileid: "82656603"
